# Platform45 Assessment

## How to Run the Project Locally

- clone the repo
- run `git clone <repository_link>`
- install all the dependencies by running `npm install` 
- after installation, start the project by running `npm start`
